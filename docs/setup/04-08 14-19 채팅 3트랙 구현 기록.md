# 채팅 3트랙 구현 기록

## 작업 목적
- `reason_codes`를 문자열에서 전용 enum으로 전환
- `_normalize_text()`를 option C 기준으로 강화
- `backend/services/chat.py`를 패키지 구조로 분리

## baseline
- branch: `kwanju`
- 실행 결과:
  - `backend/tests/unit/test_content_filter.py`: `63 passed`
  - `backend/tests/unit/test_chat_service_routing.py`: `9 passed`
  - 최종 `backend/tests/unit`: `190 passed`

## Stage 1
- 추가:
  - `backend/tests/unit/test_chat_branch_sse.py`
  - `backend/tests/integration/test_chat_branch_flow.py`
- 잠근 분기:
  - `CRISIS_ESCALATE`
  - `BLOCK`
  - `ai_consent declined`
  - `stream failure`
- 비고:
  - integration 파일은 작성 완료
  - 로컬에는 `postgres:5432` 테스트 호스트가 없어 실행 검증은 못 함

## Stage 2
- 추가:
  - `backend/services/content_filter_reason_codes.py`
- 변경:
  - `FilterReasonCode` 순수 `Enum` 도입
  - `FilterResult.reason_codes` -> `list[FilterReasonCode]`
  - `_classify_routing()`의 `health_frustration` membership을 enum 기반으로 변경

## Stage 3
- 추가:
  - `backend/tests/unit/fixtures/normalize_golden.json`
  - `backend/tests/unit/test_normalize_characterization.py`
- 변경:
  - `_normalize_text()`가 numeric token protection + zero-width 제거 + NFKC + 우회 구두점 정리를 수행

## Stage 4
- 패키지 전환 완료:
  - `backend/services/chat/__init__.py`
  - `backend/services/chat/service.py`
  - `backend/services/chat/streaming.py`
  - `backend/services/chat/persistence.py`
  - `backend/services/chat/prompting.py`
  - `backend/services/chat/enrich.py`
- 삭제:
  - `backend/services/chat.py`
- 유지한 surface:
  - `backend.services.chat.ChatService`
  - `backend.services.chat._sse_event`
  - `backend.services.chat.ROUTE_INSTRUCTIONS`
  - `backend.services.chat.EMOTIONAL_INSTRUCTION`

## 검증
- 통과:
  - `uv run ruff check backend`
  - `uv run python -m pytest backend/tests/unit -q`
  - import chain:
    - `backend.services.chat`
    - `backend.apis.v1.chat_routers`
- 미검증:
  - `backend/tests/integration/test_chat_branch_flow.py`
  - 사유: 로컬 테스트 DB 호스트 부재
