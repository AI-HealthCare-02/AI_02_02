# TTFT 추가 단축 개발 안전 체크리스트
## 사용 템플릿
- 원본: `C:\Users\mal03\Desktop\Codex\03-05 22-02 개발 안전 체크리스트 템플릿.md`
- 작성 목적: `다나아 TTFT 추가 단축 실행 플랜 vExec` 실행 전후 근거 고정

---

## 1) 요구사항 / 영향 범위 분석
- [x] 변경 목적과 성공 기준을 한 문장으로 정리했다.
- [x] 영향 범위(화면, 서버 액션, DB, 외부 연동)를 나열했다.
- [x] 현재 API / DB 계약과 충돌 여부를 확인했다.
- [x] 캐시, 권한, 성능, 동시성, 날짜/시간 처리 영향을 점검했다.

근거:
- 목적: `gpt-4o-mini` 고정 상태에서 warm steady-state `TTFT`를 줄이되 `/chat/send` SSE 계약과 DB write semantics를 유지한다.
- 영향 범위:
  - 서버: [service.py](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/backend/services/chat/service.py)
  - 프롬프트: [prompting.py](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/backend/services/chat/prompting.py)
  - 토큰 계측: [token_budget.py](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/backend/services/chat/token_budget.py)
  - 벤치: [test_bench_service_ttft.py](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/backend/tests/integration/test_bench_service_ttft.py)
  - 프론트 스트리밍 수신: [page.js](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/frontend/app/app/chat/page.js)
- 외부 계약 불변:
  - `/chat/send`
  - SSE `token/error/done`
  - `done` payload

---

## 2) 데이터 안전 (중요)
- [x] 이번 변경의 SoT(Source of Truth)를 명시했다.
- [x] 상태 전이 규칙을 정의했다.
- [x] 멱등성 규칙을 정의했다.
- [x] 동시성 / race condition 위험 구간을 확인했다.
- [x] UTC / 사용자 로컬 날짜 처리 기준을 정했다.
- [x] 실패 시 복구 경로를 정리했다.

근거:
- SoT:
  - TTFT/done 실측: [test_bench_service_ttft.py](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/backend/tests/integration/test_bench_service_ttft.py)
  - 요청 경로: [service.py](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/backend/services/chat/service.py)
- 상태 전이 규칙:
  - `validation -> content_filter -> consent/crisis/block -> session/user save -> prep -> stream -> done`
- 동시성 점검:
  - 병렬화는 `USER` message 저장 이후 `history 조회`와 `eligible_bundles 조회`에만 제한
  - `prepare_session` 이전 병렬화 금지
- 실패 복구:
  - shadow/mirror 벤치는 환경 변수로 분리
  - controlled/mirror 모두 실측 후 비교
  - code-level 최적화 효과가 미미하면 중단하고 infra 논의로 전환

---

## 3) DB / API 변경 (중요)
- [x] DB 변경 시 마이그레이션 전략이 있다.
- [x] DB 변경 시 롤백 전략이 있다.
- [x] 기존 데이터 호환성 영향을 정리했다.
- [x] API 변경 시 하위 호환성 영향과 클라이언트 영향이 정리되어 있다.
- [x] 파괴적 작업은 명시적 승인 범위 안에 있다.

근거:
- 이번 라운드는 DB 스키마 변경 없음
- 마이그레이션 없음, 롤백은 코드 revert로 충분
- API 계약 변경 없음
- 주의:
  - `ai_health` DB로 로컬 백엔드를 직접 띄우면 `chat_notification` 컬럼 불일치가 있어 browser 실측이 실패할 수 있음
  - 그래서 성능 실측 기준은 `test` DB 기반 controlled/mirror 벤치로 고정

---

## 4) 외부 연동 / 운영 경계
- [x] 외부 API 한도와 실패 조건을 반영했다.
- [x] 동기 호출과 비동기 이벤트의 책임 경계를 정했다.
- [x] 재시도 / 중복 방지 / 로깅 전략을 정리했다.
- [x] 로컬 개발 환경과 운영 환경의 차이를 기록했다.

근거:
- OpenAI는 shared client + streaming watchdog 유지
- LangGraph는 `.env` 기준 `shadow`, controlled 벤치는 `off`
- 로컬/운영 차이:
  - `.env`의 `DB_HOST=postgres`, `REDIS_HOST=redis`는 docker 네트워크 기준
  - 로컬 셸 벤치에서는 `DB_HOST=localhost`, 필요 시 `REDIS_HOST=localhost` override 필요

---

## 5) 테스트 계획 (중요)
- [x] 단위 테스트 계획 또는 기존 테스트 재사용 방안을 정리했다.
- [x] 통합 / 회귀 테스트 계획을 정리했다.
- [x] 실패 경로(권한 없음, 중복 요청, 외부 API 실패)를 검증한다.
- [x] 실행하지 못하는 테스트와 잔여 리스크를 명시한다.

근거:
- 단위:
  - `uv run python -m pytest backend/tests/unit -q`
- 통합 / 회귀:
  - `uv run python -m pytest backend/tests/integration/test_chat_branch_flow.py -q`
  - `uv run python -m pytest backend/tests/integration/test_bench_service_ttft.py -s -v`
- 잔여 리스크:
  - 브라우저 `wire_ttft_sec`, `paint_ttft_sec` 성공 샘플은 아직 확보 못 함
  - 원인: 로컬 백엔드를 `ai_health` DB로 띄울 때 `chat_notification` 컬럼 불일치로 500 발생

---

## 6) 릴리즈 준비
- [x] 모니터링 지표와 실패 시점 판단 기준을 정했다.
- [x] 롤백 트리거를 정했다.
- [x] 운영 문서 / 체크리스트 / 수정 기록을 최신화한다.

근거:
- 핵심 지표:
  - `server_ttft_sec`
  - `done_sec`
  - `generate_sec`
  - `prompt_tokens_estimate`
- 중단 기준:
  - controlled 대비 개선이 `0.04초` 미만
  - mirror에서 TTFT/done이 유의미하게 악화
  - branch/SSE 회귀 발생
- 기록 문서:
  - [04-09 22-35 TTFT 추가 단축 구현 및 검증 기록.md](/c:/Users/mal03/Desktop/레퍼런스/마지막%20웹프로젝트/docs/setup/04-09%2022-35%20TTFT%20추가%20단축%20구현%20및%20검증%20기록.md)

---

## 확인
- 작성자: Codex
- 검토자: 미정
- 확인 여부: [x] 확인 [ ] 반려
- 반려 시 사유:
