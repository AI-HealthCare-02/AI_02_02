# 개발 시작 체크리스트

## 1) 요구사항 / 영향 범위 분석
- [x] 변경 목적과 성공 기준을 한 문장으로 정리했다.
- [x] 영향 범위(화면, 서버 액션, DB, 외부 연동)를 나열했다.
- [x] 현재 API / DB 계약과 충돌 여부를 확인했다.
- [x] 캐시, 권한, 성능, 동시성, 날짜/시간 처리 영향을 점검했다.

근거:
- 목적: 노션 가이드를 윈도우 10과 현재 폴더 구조에 맞게 재해석하고, 실제 개발 경로를 명확히 만드는 것
- 영향 범위: 폴더 구조, Git 루트, 실제 Next.js 앱 경로, Claude Code 작업 기준
- 현재 구조에는 DB/API 코드가 없어서 계약 충돌은 없음
- 권한/캐시/동시성/날짜 처리 로직 변경은 없음

## 2) 데이터 안전 (중요)
- [x] 이번 변경의 SoT(Source of Truth)를 명시했다.
- [x] 상태 전이 규칙을 정의했다.
- [x] 멱등성 규칙을 정의했다.
- [x] 동시성 / race condition 위험 구간을 확인했다.
- [x] UTC / 사용자 로컬 날짜 처리 기준을 정했다.
- [x] 실패 시 복구 경로를 정리했다.

근거:
- SoT: `apps/web` 가 실제 개발 소스, `docs` 는 참고자료, `archive` 는 보관자료
- 상태 전이: 루트 혼합 구조 → 역할별 폴더 구조
- 멱등성: 이동 대상은 한 번만 재배치하며, 동일 경로가 존재하면 덮어쓰지 않음
- 동시성 위험: 단일 로컬 폴더 정리이므로 낮음
- 날짜 처리: 로컬 문서명은 한국 시간 기준 `03-30 12-32`
- 복구: 이동 전후 경로를 문서화했고, 필요 시 동일 루트 내에서 역이동 가능

## 3) DB / API 변경 (중요)
- [x] DB 변경 시 마이그레이션 전략이 있다.
- [x] DB 변경 시 롤백 전략이 있다.
- [x] 기존 데이터 호환성 영향을 정리했다.
- [x] API 변경 시 하위 호환성 영향과 클라이언트 영향이 정리되어 있다.
- [x] 파괴적 작업은 명시적 승인 범위 안에 있다.

근거:
- 이번 작업에는 DB/API 변경이 없다
- 폴더 구조 정리와 로컬 개발 환경 재배치만 수행한다

## 4) 외부 연동 / 운영 경계
- [x] 외부 API 한도와 실패 조건을 반영했다.
- [x] 동기 호출과 비동기 이벤트의 책임 경계를 정했다.
- [x] 재시도 / 중복 방지 / 로깅 전략을 정리했다.
- [x] 로컬 개발 환경과 운영 환경의 차이를 기록했다.

근거:
- 외부 연동은 GitHub remote 연결과 Notion 참고만 포함
- 현재는 로컬 환경 정리 단계이며 운영 배포는 범위 밖
- 오류는 `task_plan.md`, `progress.md` 에 기록한다

## 5) 테스트 계획 (중요)
- [x] 단위 테스트 계획 또는 기존 테스트 재사용 방안을 정리했다.
- [x] 통합 / 회귀 테스트 계획을 정리했다.
- [x] 실패 경로(권한 없음, 중복 요청, 외부 API 실패)를 검증한다.
- [x] 실행하지 못하는 테스트와 잔여 리스크를 명시한다.

근거:
- 구조 정리 검증: 이동 후 파일 경로 확인
- 개발 환경 검증: Git 루트 초기화 여부, `apps/web` 의존성 설치 여부, 빌드 가능 여부 확인
- 잔여 리스크: Node 버전 호환성, 팀 GitHub 권한, PyCharm 미설치 상태

## 6) 릴리즈 준비
- [x] 모니터링 지표와 실패 시점 판단 기준을 정했다.
- [x] 롤백 트리거를 정했다.
- [x] 운영 문서 / 체크리스트 / 수정 기록을 최신화한다.

근거:
- 현재는 릴리즈가 아니라 개발 시작 준비 단계
- 실패 기준은 `apps/web` 실행 또는 빌드 불가, Git 루트 불명확, Claude Code 작업 경로 혼선 발생
- 관련 기록은 `docs/setup` 과 planning 파일에 남긴다

## 확인
- 작성자: Codex
- 검토자:
- 확인 여부: [x] 확인 [ ] 반려
- 반려 시 사유:
