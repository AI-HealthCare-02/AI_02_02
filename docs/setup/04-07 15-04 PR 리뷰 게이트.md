# PR 리뷰 게이트

## PR 기본 정보
- 제목: DB/API 문서 정합성 복구 및 프론트 계약 변경 공지
- 이슈 / 요청 링크: Seonghee 기능정의 반영 후 문서-코드 mismatch 점검
- 변경 요약(3줄 이내):
  - 공식 API/DB 문서를 현재 코드 기준으로 정합화
  - `doc-sync-map`을 `backend/*` 기준으로 갱신
  - 프론트 계약 변경 메모와 개발/리뷰 게이트 문서 추가

---

## A. 아키텍처 / 경계 준수 게이트
- [x] 기존 모듈 경계와 소유권을 깨지 않았다.
- [x] 임시 우회 / 단기 패치 / 레이어 침범이 없다.
- [x] 캐시 / revalidate / selector 책임이 정리되어 있다.

리뷰 근거:

- 코드 구조는 변경하지 않고 문서와 운영 메모만 조정
- `backend/*` 구조를 SoT로 고정하고 문서가 이를 따라가도록 정리

---

## B. 데이터 무결성 게이트 (중요)
- [x] 정렬 / 집계 / 상태 전이의 중복 반영 위험을 막았다.
- [x] 멱등 처리 또는 기대 이전 상태 검증이 있다.
- [x] 동시성 / race condition / stale closure 위험을 줄였다.
- [x] UTC / 로컬 날짜 기준이 일관된다.

리뷰 근거:

- 이번 변경은 문서와 공지 정리라 실제 데이터 집계 로직은 미변경
- `analysis/summary`는 현재 DTO 필드만 문서에 남기고, 미구현 후보 값은 후속 항목으로 분리

---

## C. 보안 / 권한 게이트 (중요)
- [x] RLS / 권한 경계를 우회하지 않는다.
- [x] 민감 정보가 로그나 클라이언트 응답에 노출되지 않는다.
- [x] SECURITY DEFINER / service role 사용이 필요한 경우 범위가 제한되어 있다.

리뷰 근거:

- 인증/권한 로직은 수정하지 않음
- 민감 데이터 노출 규칙은 기존 문서 방향 유지

---

## D. API / DB 호환성 게이트 (중요)
- [x] API 계약 변경 시 호환성 영향이 문서화되어 있다.
- [x] DB 마이그레이션 / 롤백 계획이 준비되어 있다.
- [x] 실제 원격 / 개발 DB 계약과 코드가 일치한다.

리뷰 근거:

- 프론트 계약 변경 메모에 additive response change와 행동 규칙 변경을 분리 정리
- migration 대상: `user_settings.chat_notification`
- 머지 후 실행 명령: `docker compose exec fastapi uv run python -m aerich upgrade`

### Contract Delta

| 구분 | 내용 | 프론트 액션 |
|------|------|-------------|
| Added fields | `onboarding/status`: `gender`, `age_range`, `bmi` | null-safe 처리 |
| Added fields | `challenges/overview`: `is_fixed`, `max_active_count`, `remaining_active_slots` | 카드/슬롯 UI 반영 |
| New endpoints | `PATCH /auth/consent`, `GET /health/weekly`, `GET /risk/history`, settings 3개 API | 연결 시 문서 기준 사용 |
| Behavior changes | 건강 질문 노출이 `chat_notification`, `max_bundles_per_day`, `preferred_times`를 반영 | “항상 질문 노출” 가정 금지 |
| Migration required | `user_settings.chat_notification` 컬럼 추가 | 머지 후 migration 확인 |
| Not yet implemented | `risk_badge`, `overall_grade`, `coach_summary` | real 데이터로 가정 금지 |

---

## E. 테스트 게이트 (중요)
- [x] 단위 테스트 통과 또는 미실행 사유가 있다.
- [x] 통합 / 회귀 테스트 통과 또는 미실행 사유가 있다.
- [x] 실패 경로 테스트가 확인되었다.

테스트 결과:

- 정적 정합성 확인 예정:
  - `analysis.py` 응답 필드와 API 문서 일치
  - `doc-sync-map.md` 에 `app/*` 경로 잔존 여부 확인
  - `coach_summary`, `risk_badge`, `overall_grade`가 “현재 구현”처럼 남아 있지 않은지 확인
- 코드 회귀 테스트는 기존 backend 변경 커밋 기준 결과를 참고
- migration smoke는 컨테이너 환경에서 최종 재확인 필요

---

## F. 운영 준비 게이트
- [x] 모니터링 / 알림 / 로그 점검 기준이 있다.
- [x] 롤백 조건과 실행 절차가 정리되어 있다.
- [x] 문서(체크리스트 / 수정 기록 / 운영 가이드)가 갱신되었다.

운영 메모:

- merge blocker:
  - 문서가 코드보다 앞서 있으면 안 됨
  - 프론트 계약 메모 누락 시 머지 금지
  - migration 실행 명령 누락 시 머지 금지

---

## 최종 결정
- [ ] Approve
- [ ] Request Changes

결정 사유:
