# PR 리뷰 게이트
## PR 기본 정보
- 제목: [codex] finish health card sync and dual-llm chat flow
- 이슈 / 요청 링크: 사용자 직접 요청
- 변경 요약(3줄 이내):
  - 건강 기록 direct save, 질문 카드, 오른쪽 패널, 채팅 상태 동기화를 하나의 흐름으로 정리했다.
  - AI 채팅이 남은 질문/다음 카드 상태를 이해해 설명할 수 있도록 app context를 확장했다.
  - Gemma primary + OpenAI fallback 구조와 관련 설정/테스트를 추가했다.

---

## A. 아키텍처 / 경계 준수 게이트
- [x] 기존 모듈 경계와 소유권을 깨지 않았다.
- [x] 임시 우회 / 단기 패치 / 레이어 침범이 없다.
- [x] 캐시 / revalidate / selector 책임이 정리되어 있다.

리뷰 근거:
- 백엔드는 `chat`, `health_daily`, `health_question` 서비스 경계를 유지한 채 DTO만 확장했다.
- 프론트는 `page.js`와 `InlineHealthQuestionCard.js` 안에서 화면 동기화 책임을 모으고, 서버 계산값은 `pending_questions`, `card_availability` 응답을 그대로 사용한다.
- AI 앱 이해 로직은 `backend/services/chat/app_context.py`로 분리되어 채팅 본문 생성과 섞이지 않는다.

---

## B. 데이터 무결성 게이트 (중요)
- [x] 정렬 / 집계 / 상태 전이의 중복 반영 위험을 막았다.
- [x] 멱등 처리 또는 기대 이전 상태 검증이 있다.
- [x] 동시성 / race condition / stale closure 위험을 줄였다.
- [x] UTC / 로컬 날짜 기준이 일관된다.

리뷰 근거:
- `health_question.py`가 bundle 순서, unanswered field, next card 계산을 서버 기준으로 일원화한다.
- `frontend/app/app/chat/page.js`는 direct save 중 서버 응답이 뒤늦게 와도 사용자 입력 상태를 덮어쓰지 않도록 `directSaveInflightRef`, `lastServerLogRef`를 사용한다.
- `health_daily.py`는 오늘 날짜 판단을 `datetime.now(tz=config.TIMEZONE).date()` 기준으로 맞춰 자정 이후 미래 날짜 오판을 막는다.

---

## C. 보안 / 권한 게이트 (중요)
- [x] RLS / 권한 경계를 우회하지 않는다.
- [x] 민감 정보가 로그나 클라이언트 응답에 노출되지 않는다.
- [x] SECURITY DEFINER / service role 사용이 필요한 경우 범위가 제한되어 있다.

리뷰 근거:
- 기존 인증/인가 경로 위에서만 health patch, chat answer API를 사용한다.
- `test_chat_app_context.py` 기준으로 app context 로그는 redaction 처리된다.
- 이번 변경에는 DB 권한 모델이나 service role 확장이 없다.

---

## D. API / DB 호환성 게이트 (중요)
- [x] API 계약 변경 시 호환성 영향이 문서화되어 있다.
- [x] DB 마이그레이션 / 롤백 계획이 준비되어 있다.
- [x] 실제 원격 / 개발 DB 계약과 코드가 일치한다.

리뷰 근거:
- `HealthAnswerResponse`, `DailyLogResponse`에 `daily_log`, `pending_questions`, `card_availability`를 추가했지만 모두 추가 필드라 기존 클라이언트 호환성은 유지된다.
- DB 스키마 변경은 없어서 마이그레이션은 필요 없다. 롤백은 애플리케이션 코드 revert로 가능하다.
- 테스트는 로컬 postgres(`DB_HOST=127.0.0.1`) 기준으로 통과했다.

---

## E. 테스트 게이트 (중요)
- [x] 단위 테스트 통과 또는 미실행 사유가 있다.
- [x] 통합 / 회귀 테스트 통과 또는 미실행 사유가 있다.
- [x] 실패 경로 테스트가 확인되었다.

테스트 결과:
- 성공: `frontend/npm run build`
- 성공: `.venv\Scripts\python.exe -m pytest backend/tests/unit/test_chat_llm_provider.py backend/tests/unit/test_chat_app_context.py backend/tests/integration/test_chat_app_context_health_summary.py backend/tests/integration/test_chat_health_answer_sync.py backend/tests/integration/test_health_question_sequence.py backend/tests/integration/test_health_daily.py backend/tests/integration/test_chat_phase1_guards.py`
- 성공: `.venv\Scripts\python.exe -m compileall backend`
- 참고: 윈도우 기본 셸에서는 `DB_HOST=postgres` 이름 해석이 실패해서, 통합 테스트는 `DB_HOST=127.0.0.1`로 실행했다.

---

## F. 운영 준비 게이트
- [x] 모니터링 / 알림 / 로그 점검 기준이 있다.
- [x] 롤백 조건과 실행 절차가 정리되어 있다.
- [x] 문서(체크리스트 / 수정 기록 / 운영 가이드)가 갱신되었다.

운영 메모:
- LLM warmup/logging 키가 OpenAI 단일 기준에서 LLM 공통 기준으로 정리됐다.
- direct save 재현 시 확인할 핵심 로그는 `today_log_save_failed`, `chat_app_context`, `chat_llm_usage`다.
- 문제가 생기면 이번 커밋 revert 후 OpenAI 단일 경로와 기존 direct save 흐름으로 되돌릴 수 있다.

---

## 최종 결정
- [x] Approve
- [ ] Request Changes

결정 사유:
- 사용자 체감 핵심 흐름인 카드 질문, 오른쪽 패널 direct save, AI 상태 설명, Gemma/OpenAI 이중 경로가 함께 정리되었고, 관련 빌드/테스트가 확인되었다.
